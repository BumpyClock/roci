# Document provider DX + custom provider example

## Scope
- README: explain `roci` vs `roci-core` + `roci-providers` usage.
- Add an example showing a custom provider registration.
- Update architecture docs and `LEARNINGS.md`.

## Acceptance criteria
1) Docs show both “batteries-included” and “explicit wiring” paths.
2) Custom provider example compiles.
