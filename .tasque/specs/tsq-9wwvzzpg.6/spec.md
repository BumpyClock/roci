# Add registry + split integration tests

## Scope
- Unit tests for registry selection + custom provider.
- Build/test matrix for `roci-core`, `roci-providers`, and `roci`.

## Acceptance criteria
1) Tests pass for each crate individually.
2) Registry tests cover error cases and custom provider success path.
