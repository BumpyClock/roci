//! Built-in tool implementations for roci agents.
//!
//! This crate provides the standard coding tools (shell, read_file, write_file,
//! list_directory, grep) extracted from the roci core crate. Access them via
//! [`builtin::all_tools()`].

pub mod builtin;
